# ubuntu をインストールして最初にすること

    インストールされているソフトウェアの更新
    '$ sudo apt update'
    '$ sudo apt uprade'

    WSL上でwindowsフォルダにアクセスするためのショートカットリンク
    '$ ln -s /mnt/c/Users/{username}/{folder} ~/{folder}'    